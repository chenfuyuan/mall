package ${package}.${moduleName}.domain.specification;

import com.learn.project.common.core.domain.AbstractSpecification;
import ${package}.${moduleName}.domain.model.${classname}.${className};
import ${package}.${moduleName}.domain.model.${classname}.${className}Repository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

/**
 * ${comments}-创建时校验类
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Component
public class ${className}CreateSpecification extends AbstractSpecification<${className}> {

    /**
     * 仓储服务
     */
    private final ${className}Repository ${classname}Repository;

    @Autowired
    public ${className}CreateSpecification(${className}Repository ${classname}Repository) {
        this.${classname}Repository = ${classname}Repository;
    }

    @Override
    public boolean isSatisfiedBy(${className} ${classname}) {
        return true;
    }
}
